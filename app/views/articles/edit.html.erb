<%= form_with(model: @article, local: true) do |form| %>

    <div>
        <br/>
        <%=form.label :title %>
        <%=form.text_field :title %>
    </div>

    <div>
        <br/>
        <%=form.label :content %>
        <%=form.rich_text_area :content %>
    </div>

    <div>
        <%= form.submit  %>
    </div>

<%end%>